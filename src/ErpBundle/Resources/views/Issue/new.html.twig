{% extends '@Erp/layout.html.twig' %}

{% block body -%}
    {{ parent() }}
    <h1>Issue creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('issue') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
