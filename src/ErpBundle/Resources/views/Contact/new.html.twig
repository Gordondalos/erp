{% extends '@Erp/layout.html.twig' %}
{% block body %}
    {{ parent() }}
    <h1>Contact creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('contact') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
