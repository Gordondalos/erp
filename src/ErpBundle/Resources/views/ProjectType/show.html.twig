{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ProjectType</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Typename</th>
                <td>{{ entity.typeName }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('projecttype') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('projecttype_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
